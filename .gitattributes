# 仓库内统一使用 LF 行尾。
#
# 这一点对本项目的部署脚本是硬要求：
# shell 脚本与 systemd 单元文件若带 CRLF，在 Linux 上会直接报错
#   bash: $'\r': command not found
# 导致 install-centos.sh / update.sh 完全跑不起来。
#
# eol=lf 保证检出到任何平台都是 LF。

* text=auto eol=lf

*.sh      text eol=lf
*.service text eol=lf
*.conf    text eol=lf
*.yml     text eol=lf
*.yaml    text eol=lf
*.js      text eol=lf
*.json    text eol=lf
*.md      text eol=lf
*.css     text eol=lf
*.html    text eol=lf
.env.example text eol=lf

# 二进制文件不做任何转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
