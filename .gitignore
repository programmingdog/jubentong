# 依赖
node_modules/

# 运行时数据（含签名密钥 dl_secret，绝不能上传）
.data/

# 环境变量（含 API Keys，绝不能上传；模板 .env.example 需要上传）
.env
!.env.example

# 本地工作区数据（记忆、会话等，不属于项目源码）
.workbuddy/

# 日志与系统文件
*.log
.DS_Store
Thumbs.db

# 压缩包 / 备份
*.rar
*.zip
*.7z
*.tar.gz
*.bak

# 调试产物
research/raw_video.json
